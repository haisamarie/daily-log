import Hero from "@/components/Hero";
import styles from "./styles.module.scss";

const Home = () => {
  return (
    <main className={styles["wrap"]}>
      <Hero />
    </main>
  );
};
export default Home;
