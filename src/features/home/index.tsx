import Hero from "@/components/Hero";
import styles from "./style.module.css";

const Home = () => {
  return (
    <main className={styles["wrap"]}>
      <Hero />
    </main>
  );
};
export default Home;
